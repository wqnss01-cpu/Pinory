import{useState}from'react';import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{AnimatePresence,motion}from'motion/react';import{Bell,Settings,UserPlus,Plus,MapPinned,Grid2X2,Edit3,X,Check,Layers3}from'lucide-react';import type{User}from'@pinory/shared';import{api}from'../lib/api';import{telegram}from'../lib/telegram';import{t}from'../i18n/ru';import{useAppStore}from'../store';import{EntryCard}from'../components/EntryCard';import{Logo}from'../components/Logo';
export function ProfileScreen({user}:{user:User}){const[tab,setTab]=useState<'entries'|'collections'>('entries');const[editing,setEditing]=useState(false);const[creating,setCreating]=useState(false);const notifications=useAppStore((s)=>s.setNotificationsOpen);const selectCollection=useAppStore((s)=>s.selectCollection);const entries=useQuery({queryKey:['user-entries',user.id],queryFn:()=>api.userEntries(user.id)});const collections=useQuery({queryKey:['user-collections',user.id],queryFn:()=>api.userCollections(user.id)});const invite=async()=>{const data=await api.invite();telegram.share(`${location.origin}?ref=${data.start_parameter}`,t.inviteCta);};return <section className="page profile-page"><header className="page-header"><Logo/><div><button className="icon-button" onClick={()=>notifications(true)}><Bell/></button><button className="icon-button" onClick={()=>setEditing(true)}><Settings/></button></div></header><div className="profile-hero"><div className="profile-orbit"><i/><span>{user.avatarUrl?<img src={user.avatarUrl}/>:user.displayName[0]}</span><b><MapPinned/></b></div><div><span className="eyebrow">{t.profile.myAtlas}</span><h1>{user.displayName}</h1><p>{[user.telegramUsername?`@${user.telegramUsername}`:null,user.homeCity].filter(Boolean).join(' · ')||'Профиль Pinory'}</p></div></div><p className={`profile-bio ${user.bio?'':'empty-bio'}`}>{user.bio??'Добавьте пару слов о себе и своих путешествиях.'}</p><div className="profile-actions"><button className="primary" onClick={()=>setEditing(true)}><Edit3/>{t.profile.edit}</button><button onClick={invite}><UserPlus/>{t.profile.invite}</button></div><div className="profile-stats"><button><strong>{user.stats?.visited??entries.data?.items.filter((item)=>item.entryType==='VISITED').length??0}</strong><span>{t.profile.visited}</span></button><button><strong>{user.stats?.wishlist??entries.data?.items.filter((item)=>item.entryType==='WISHLIST').length??0}</strong><span>{t.profile.wishlist}</span></button><button><strong>{collections.data?.items.length??0}</strong><span>{t.profile.collections}</span></button></div><div className="social-stats"><span><b>{user.stats?.followers??0}</b> {t.profile.followers}</span><i/><span><b>{user.stats?.friends??0}</b> {t.profile.friends}</span><i/><span><b>{user.stats?.following??0}</b> {t.profile.following}</span></div><div className="profile-tabs"><button className={tab==='entries'?'active':''} onClick={()=>setTab('entries')}><MapPinned/>{t.nav.map}</button><button className={tab==='collections'?'active':''} onClick={()=>setTab('collections')}><Grid2X2/>{t.profile.collections}</button></div><AnimatePresence mode="wait"><motion.div key={tab} initial={{opacity:0,x:12}} animate={{opacity:1,x:0}} exit={{opacity:0,x:-10}}>{tab==='entries'?<div className="profile-grid">{entries.data?.items.map((entry)=><EntryCard key={entry.id} entry={entry}/>)}{entries.data?.items.length===0&&<div className="profile-empty"><MapPinned/><p>Ваши места появятся здесь.</p></div>}</div>:<div className="collection-grid"><button className="new-collection" onClick={()=>setCreating(true)}><Plus/><span>{t.profile.newCollection}</span></button>{collections.data?.items.map((collection)=><button className="collection-card" key={collection.id} onClick={()=>selectCollection(collection.id)}><div>{collection.coverUrl?<img src={collection.coverUrl}/>:<Layers3/>}<i/></div><h3>{collection.title}</h3><p>{collection.placesCount} мест</p></button>)}</div>}</motion.div></AnimatePresence>{editing&&<EditProfile user={user} close={()=>setEditing(false)}/>} {creating&&<CreateCollection close={()=>setCreating(false)}/>}</section>}
function Sheet({children}:{children:React.ReactNode}){return <motion.div className="sheet-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.section className="bottom-sheet compact" initial={{y:'100%'}} animate={{y:0}} exit={{y:'100%'}} transition={{type:'spring',stiffness:320,damping:32}}><div className="sheet-handle"/>{children}</motion.section></motion.div>}
function EditProfile({user,close}:{user:User;close:()=>void}){const[name,setName]=useState(user.displayName);const[bio,setBio]=useState(user.bio??'');const[city,setCity]=useState(user.homeCity??'');const qc=useQueryClient();const mutation=useMutation({mutationFn:()=>api.updateMe({displayName:name,bio,homeCity:city}),onSuccess:(data)=>{qc.setQueryData(['me'],data);close();}});return <Sheet><header className="sheet-header"><h2>{t.profile.edit}</h2><button className="icon-button" onClick={close}><X/></button></header><label className="field"><span>Имя</span><input value={name} onChange={(event)=>setName(event.target.value)}/></label><label className="field"><span>О себе</span><textarea value={bio} onChange={(event)=>setBio(event.target.value)}/></label><label className="field"><span>Город</span><input value={city} onChange={(event)=>setCity(event.target.value)}/></label><button className="primary full" onClick={()=>mutation.mutate()}><Check/>Сохранить</button></Sheet>}
function CreateCollection({close}:{close:()=>void}){const[title,setTitle]=useState('');const[description,setDescription]=useState('');const qc=useQueryClient();const selectCollection=useAppStore((s)=>s.selectCollection);const mutation=useMutation({mutationFn:()=>api.createCollection({title,description,visibility:'PUBLIC',entryIds:[]}),onSuccess:(collection)=>{void qc.invalidateQueries({queryKey:['user-collections']});close();selectCollection(collection.id);}});return <Sheet><header className="sheet-header"><h2>{t.profile.newCollection}</h2><button className="icon-button" onClick={close}><X/></button></header><label className="field"><span>Название</span><input value={title} onChange={(event)=>setTitle(event.target.value)} placeholder="Например, выходные без плана"/></label><label className="field"><span>Описание</span><textarea value={description} onChange={(event)=>setDescription(event.target.value)}/></label><button disabled={title.trim().length<2||mutation.isPending} className="primary full" onClick={()=>mutation.mutate()}><Plus/>Создать подборку</button></Sheet>}
