import{useState}from'react';import{useQuery}from'@tanstack/react-query';import{Bell,Compass,RefreshCw}from'lucide-react';import{api}from'../lib/api';import{t}from'../i18n/ru';import{useAppStore}from'../store';import{EntryCard}from'../components/EntryCard';import{Logo}from'../components/Logo';
export function FeedScreen(){const[kind,setKind]=useState<'following'|'nearby'|'global'>('following');const openNotifications=useAppStore((s)=>s.setNotificationsOpen);const query=useQuery({queryKey:['feed',kind],queryFn:()=>api.feed(kind)});return <section className="page feed-page"><header className="page-header"><Logo/><button className="icon-button notification-button" onClick={()=>openNotifications(true)}><Bell/><i/></button></header><div className="title-row"><div><span className="eyebrow">PINORY PULSE</span><h1>{t.feed.title}</h1></div><Compass/></div><div className="segmented feed-tabs"><button className={kind==='following'?'active':''} onClick={()=>setKind('following')}>{t.feed.following}</button><button className={kind==='nearby'?'active':''} onClick={()=>setKind('nearby')}>{t.feed.nearby}</button><button className={kind==='global'?'active':''} onClick={()=>setKind('global')}>{t.feed.global}</button></div>{query.isLoading?<div className="card-list"><div className="skeleton-card"/><div className="skeleton-card"/></div>:query.isError?<div className="state-card"><RefreshCw/><h3>{t.error.title}</h3><button onClick={()=>query.refetch()}>{t.retry}</button></div>:query.data?.items.length?<div className="card-list">{query.data.items.map((entry)=><EntryCard key={entry.id} entry={entry} wide/>)}</div>:<div className="state-card illustrated"><span>🪁</span><h3>{t.feed.empty}</h3><p>{t.feed.emptyText}</p><button onClick={()=>setKind('nearby')}>{t.feed.nearby}</button></div>}</section>}
