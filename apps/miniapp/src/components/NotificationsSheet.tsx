import{useQuery}from'@tanstack/react-query';import{Bell,Bookmark,Heart,Link2,MapPin,MessageCircle,Route,UserPlus,Users,X}from'lucide-react';import{api}from'../lib/api';import{useAppStore}from'../store';
const copy:Record<string,string>={NEW_COMMENT:'оставил комментарий',MUTUAL_FOLLOW:'теперь ваш друг',NEW_FOLLOWER:'подписался на ваш атлас',WANT_HERE:'хочет побывать в вашем месте',ALSO_VISITED:'тоже был в вашем месте',ENTRY_LIKED:'оценил ваше воспоминание',COLLECTION_SAVED:'сохранил вашу подборку',COLLECTION_COMMENT:'написал в обсуждении подборки',COLLECTION_FOLLOWED:'подписался на подборку',COLLECTION_INVITE:'пригласил в общую подборку',COMPANION_INVITE:'добавил вас к воспоминанию',COMPANION_CONFIRMED:'подтвердил совместное посещение',MEMORY_MERGED:'объединил воспоминания'};const icon=(type:string)=>type==='NEW_COMMENT'?<MessageCircle/>:type.includes('COLLECTION')?<Route/>:type==='WANT_HERE'?<Bookmark/>:type==='ENTRY_LIKED'?<Heart/>:type.includes('COMPANION')?<Users/>:type==='MEMORY_MERGED'?<Link2/>:type.includes('FOLLOW')?<UserPlus/>:<MapPin/>;
export function NotificationsSheet(){const close=useAppStore(s=>s.setNotificationsOpen);const select=useAppStore(s=>s.select);const collection=useAppStore(s=>s.selectCollection);const profile=useAppStore(s=>s.openProfile);const query=useQuery({queryKey:['notifications'],queryFn:api.notifications});const open=async(n:any)=>{close(false);if(n.entity_type==='ENTRY'&&n.entity_id){const entry=await api.entry(n.entity_id).catch(()=>null);if(entry)select(entry)}else if(n.entity_type==='COLLECTION')collection(n.entity_id);else if((n.entity_type==='USER'||n.entity_type==='PROFILE')&&n.entity_id)profile(n.entity_id)};return <div className="sheet-backdrop"><section className="bottom-sheet notifications-sheet"><div className="sheet-handle"/><header className="sheet-header"><div><span className="eyebrow">PINORY PULSE</span><h2>События</h2></div><button className="icon-button" onClick={()=>close(false)}><X/></button></header><div className="notification-list">{query.data?.items.length?query.data.items.map((n:any)=><button key={n.id} className={n.is_read?'':'unread'} onClick={()=>void open(n)}><span>{icon(n.type)}</span><p><strong>{n.actor_name??'Pinory'}</strong>{copy[n.type]??'оставил новое событие'}<small>Открыть</small></p><time>{new Date(n.created_at).toLocaleDateString('ru-RU',{day:'numeric',month:'short'})}</time></button>):<div className="state-card"><Bell/><h3>Новых событий нет</h3><p>Здесь появятся действия друзей и совместные планы.</p></div>}</div></section></div>}
