import {create} from 'zustand';import type {MapEntry} from '@pinory/shared';
export type Screen='map'|'feed'|'search'|'profile';
interface State{screen:Screen;selected:MapEntry|null;selectedCollectionId:string|null;addOpen:boolean;notificationsOpen:boolean;layers:string[];entryTypes:string[];mapCenter:{lat:number;lng:number};setScreen:(screen:Screen)=>void;select:(entry:MapEntry|null)=>void;selectCollection:(id:string|null)=>void;setAddOpen:(open:boolean)=>void;setNotificationsOpen:(open:boolean)=>void;toggleLayer:(layer:string)=>void;toggleType:(type:string)=>void;setMapCenter:(center:{lat:number;lng:number})=>void;}
export const useAppStore=create<State>((set)=>({screen:'map',selected:null,selectedCollectionId:null,addOpen:false,notificationsOpen:false,layers:['mine','friends','public'],entryTypes:['VISITED','WISHLIST'],mapCenter:JSON.parse(localStorage.getItem('pinory.mapCenter')??'{"lat":55.7512,"lng":37.6184}'),setScreen:(screen)=>set({screen,selected:null,selectedCollectionId:null}),select:(selected)=>set({selected}),selectCollection:(selectedCollectionId)=>set({selectedCollectionId}),setAddOpen:(addOpen)=>set({addOpen}),setNotificationsOpen:(notificationsOpen)=>set({notificationsOpen}),toggleLayer:(layer)=>set((s)=>({layers:s.layers.includes(layer)?s.layers.filter((x)=>x!==layer):[...s.layers,layer]})),toggleType:(type)=>set((s)=>({entryTypes:s.entryTypes.includes(type)?s.entryTypes.filter((x)=>x!==type):[...s.entryTypes,type]})),setMapCenter:(mapCenter)=>{localStorage.setItem('pinory.mapCenter',JSON.stringify(mapCenter));set({mapCenter});}}));
